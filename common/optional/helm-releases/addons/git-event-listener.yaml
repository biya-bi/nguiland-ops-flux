apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: git-event-listener
  namespace: ${ENVIRONMENT}
spec:
  interval: 10m
  chart:
    spec:
      chart: org.nguiland.infra/git-event-listener
      version: 0.1.0
      sourceRef:
        kind: HelmRepository
        name: artifactory-oci
      interval: 10m
  install:
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: 3
  values:
    image:
      repository: ${ARTIFACTORY_JCR_HOST}/docker-local/org.nguiland.infra/git-event-listener
